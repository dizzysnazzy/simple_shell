#include <stdio.h>

/**
* main - prints program name
* @argc: the number of arguments
* @argv: an array containing the programs's arguments
* Return: 0 always for success
*/

int main(argc, char *argv[])
{
printf("%s\n",argv[argc - 1]);
return 0;
}

